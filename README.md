# header.nvim

> A Tool, to create header files and CPP files at the same time

## What it does
- Prompting you to give a name
- Creating `includes` and `src` folder if you do not have one
- creating a basic `.h` and `.cpp` file with the name you gave

## How to run
```lua
require("header").create()
```
